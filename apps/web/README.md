# web

Reserved slot for the Callimachus marketing + download site.

Not built yet. When it lands it should follow the Beta Bots default stack
(Next.js 16 App Router, Tailwind v4, shadcn/Base UI, Biome) and join the
Turborepo pipeline automatically — drop a `package.json` here with `build` /
`dev` / `lint` / `typecheck` scripts and it's picked up by `turbo`.

Hosting target: Vercel.
